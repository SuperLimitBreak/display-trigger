FROM nginx:mainline-alpine

# Placeholder for building nginx container with all the dependent files baked in.
# TODO: all of the dynamicly mounted `volumes` in docker-compose.yml need to be COPYed
