/* global HOST_STATIC_PORT */

/*
DEPRICATED
import {staticUrl} from 'src/utils/utils';
describe('Utils - staticUrl', function() {

    it('Should attach the static hostname to relative paths',()=>{
        expect(staticUrl('/assets/test.mp4')).toBe(`http://localhost:${HOST_STATIC_PORT}/assets/test.mp4`);
    });

    it('Should pass through absolute urls',()=>{
        expect(staticUrl('http://video.com/test.mp4')).toBe('http://video.com/test.mp4');
    });

});
*/