import { TimelineMax } from 'gsap';

export class text {
    constructor(element, kwargs) {
        console.log('Greensock is ', TimelineMax);
    }
    
    text(msg) {
        
    }
}
text.className = 'text';